# research-department

Research department -- information synthesis and analysis, matching core/capabilities/planner.js's "research" capability catalog entry. Distinct from core/research/engine.js (Phase 29), which is the core, already-real research/citation engine this department's agents would actually use once implemented.

**This package was generated by core/capabilities/builder.js (Phase 27)
as a SKELETON.** Every agent/tool file it created is a real, loadable
placeholder that throws/says so if actually used -- none of it is
working capability yet. Before installing this for real use:

1. Replace each agent's system prompt in `agents/` with real
   responsibilities.
2. Replace each tool handler's body in `tools/` with real behavior.
3. Fill in `department/manager.js` if this department needs anything beyond the standard DepartmentManager base -- most don't.

## Agents
- `ResearchAgent` (Research Agent) -- agents/researchagent.js
- `AnalysisAgent` (Analysis Agent) -- agents/analysisagent.js

## Tools
- `research.dept.synthesize` -- tools/research.dept.synthesize.js

## Memory

This package's own memories should be tagged with
`core/capabilities/activation.js`'s `namespaceTagFor("research-department")` (i.e.
the tag `"capability:research-department"`) so they're filterable without a new
storage mechanism -- see that file's own comment.

## Dashboard

There is no dynamic dashboard-panel plugin system yet (see
docs/NEXT_STEPS.md) -- a real dashboard view for this package means
extending dashboard/frontend/index.html and app.js directly, the same
way every existing panel was added.

## Installing

```js
const installer = require("core/capabilities/installer");
installer.install("packages/research-department");
```
